GridConf
- double or int?
- labelWidth? mejor hacer un svg dentro del svg padre que ya me ponga la coordenada 0,0 donde arranca el primer step.
- roundWidth (donde pongo el vertice? a la mitad del round?)
- processHeigth
- arrow heads size
- cross size
- letter size


Tiene que haber una relacion entre processHeigth y roundWidth para que no se deforme

processHeigth * roundWidht = round area

donde pongo el text del svg



Problemas
- multiples send en el mismo (turno,proceso)
- multiples indications en el mismo (turno,proceso)
- self deliver + ind (creo que van a ir siempre juntos porque no se puede dropear ese deliver)

Must implement
- i18n
- show grouped actions (e.g. n sends from same process at same index)
- download icons and fonts

Could implement
- que aparezca el scroll solo cuando hay rounds para ver. Para esto voy a necesitar el current round (ya sea para pintar mas rounds o para
  poner el overflow: scroll si pinto los rounds de manera anticipada, ej: 100000 para estar seguro q van a entrar).
- distintos estilos de arrow head para indications y request (como los que estan en el libro)
- filtros (por proceso, por tipo de mensaje, etc)
- show grid on off
- type safe action selection. El stack de core deberia darme un objeto que me diga que tipos de request entiende, y pasarle esto
  al div para q pinte los input necesarios
- animacion cuando cambia el estado de un proceso
- guardar en local storage el avance
- guardar el estado del level si cambio de level
- restart level
- hide left menu
- cambiar el estilo o el color de la linea de tiempo cuando se cae el nodo (rojo o dashed)
- cambiar el estilo de la linea a medida que "avanza" el tick
- un primer nivel dummy con modals que indiquen como usar la app

Investigate
- EventStream[GridConf]
  conviene que GridConf sea directamente quien hace las arrows?
  Entiendo que en este casi si cambia el gridConf tiene que reinsertar elementos en el dom

  o conviene que GridConf devuela roundWidth como un Stream[Int] en lugar de un Int?


Laminar questions
- contramap
- router (como updatear el hash si el q viene no es valido!)
- como resetear un form dsp de submitear? (se puede dentro del map?)
- throttle input changes
- como funciona la magica de inContext + addObserver a una signal + onClick signal.now
- writeBus vs observer?
- var update vs set vs setCurrentValue
- Observer.apply[String](l => someVar.update(_ ::: List(l))) es valido para setear el valor de las Vars ?



Proximos pasos
- Implementar correctamente la transformacion de Undelivered a Delivered (parece que hay un paso anterior a que sean actions)
- Implementar requestType por level
- Implementar request build por level
- Implementar status show por level
- Implementar conditions
